<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <title>Create New Post</title>
        <script src="https://cdn.tailwindcss.com"></script>
    </head>
    <body class="bg-gray-100 text-gray-900 min-h-screen">
        <!-- Header -->
        <header class="bg-white shadow p-6">
            <div class="max-w-4xl mx-auto flex justify-between items-center">
                <div>
                    <a href="/">
                        <h1 class="text-3xl font-bold text-indigo-600">
                            Members Only
                        </h1></a
                    >
                    <p class="text-gray-600">
                        Exclusive content for registered users
                    </p>
                </div>

                <div class="text-right">
                    <% if (user) { %>
                    <p class="text-gray-700 font-medium mb-1">
                        Hello,
                        <a
                            href="<%= `/users/${user.id}` %>"
                            class="text-indigo-600 hover:underline"
                            ><%= `${user.first_name} ${user.last_name}` %></a
                        >
                    </p>
                    <div class="flex flex-col gap-2">
                        <a
                            href="/auth/logout"
                            class="text-sm text-indigo-600 hover:underline"
                            >Logout</a
                        >
                        <a
                            href="/post/new"
                            class="text-sm text-indigo-600 hover:underline"
                            >New Post</a
                        >
                    </div>
                    <% } else { %>
                    <a
                        href="/auth/login"
                        class="text-sm text-indigo-600 hover:underline"
                        >Login</a
                    >
                    <% } %>
                </div>
            </div>
        </header>

        <!-- Main content -->
        <main class="max-w-4xl mx-auto mt-10 px-4 pb-20">
            <h2 class="text-2xl font-semibold mb-6">Create New Post</h2>
            <div class="bg-white p-8 rounded-2xl shadow">
                <form action="/post/new" method="POST" class="space-y-5">
                    <div>
                        <label
                            for="title"
                            class="block text-sm font-medium text-gray-700"
                            >Title</label
                        >
                        <input
                            type="text"
                            name="title"
                            id="title"
                            required
                            class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500"
                        />
                    </div>
                    <div>
                        <label
                            for="content"
                            class="block text-sm font-medium text-gray-700"
                            >Content</label
                        >
                        <textarea
                            name="content"
                            id="content"
                            rows="10"
                            required
                            class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500 resize-none"
                        ></textarea>
                    </div>
                    <% if (message) { %>
                    <p class="text-red-500 text-sm"><%= message %></p>
                    <% } %>
                    <button
                        type="submit"
                        class="w-full bg-indigo-600 text-white py-2 px-4 rounded-md hover:bg-indigo-700 transition"
                    >
                        Publish Post
                    </button>
                </form>
            </div>
        </main>
    </body>
</html>
